// Generated by scripts/extract.mjs. Do not edit; fix upstream and re-extract.
/**
 * Structural types for the Graph resources this client reads.
 *
 * These describe the fields requested in `constants.ts`; they are not runtime
 * validated. Meta adds fields freely, and validating responses would break on
 * additions that are harmless. Fields are optional because Graph omits rather
 * than nulls anything the token lacks permission to see.
 */

export interface AdAccount {
  id: string
  name?: string
  account_status?: number
  currency?: string
  timezone_name?: string
  created_time?: string
  amount_spent?: string
  spend_cap?: string
  business_id?: string
}

export interface Campaign {
  id: string
  name?: string
  status?: string
  objective?: string
  daily_budget?: string
  lifetime_budget?: string
  start_time?: string
  stop_time?: string
  created_time?: string
  updated_time?: string
  effective_status?: string
}

export interface AdSet {
  id: string
  name?: string
  status?: string
  campaign_id?: string
  daily_budget?: string
  lifetime_budget?: string
  optimization_goal?: string
  created_time?: string
  updated_time?: string
}

export interface Ad {
  id: string
  name?: string
  adset_id?: string
  status?: string
  created_time?: string
  updated_time?: string
  effective_status?: string
}

/** A single action/conversion count within an insights row. */
export interface InsightAction {
  action_type: string
  value: string
}

/**
 * Monetary and count metrics are returned as strings by Graph, not numbers.
 * Converting them is the caller's decision — the outcome layer needs to control
 * rounding and currency handling explicitly (tool-design checklist item 4).
 */
export interface CampaignInsights {
  impressions?: string
  clicks?: string
  spend?: string
  reach?: string
  frequency?: string
  ctr?: string
  cpc?: string
  cpp?: string
  actions?: InsightAction[]
  conversions?: InsightAction[]
  action_values?: InsightAction[]
  date_start?: string
  date_stop?: string
  [breakdown: string]: unknown
}

/** Graph date presets accepted by the insights endpoint. */
export type DatePreset =
  | "today"
  | "yesterday"
  | "this_week_mon_today"
  | "last_7d"
  | "last_14d"
  | "last_30d"
  | "last_90d"
  | "this_month"
  | "last_month"
  | "this_quarter"
  | "maximum"

export type InsightBreakdown =
  | "age"
  | "gender"
  | "country"
  | "region"
  | "publisher_platform"
  | "platform_position"
  | "device_platform"
  | "impression_device"

export interface AdCreative {
  id: string
  /** The Page post an ad actually renders, including inline-created dark posts. */
  effective_object_story_id?: string
  object_story_id?: string
}
