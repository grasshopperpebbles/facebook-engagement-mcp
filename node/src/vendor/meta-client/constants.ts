// Generated by scripts/extract.mjs. Do not edit; fix upstream and re-extract.
/**
 * Graph API version. Pinned deliberately — see ADR 0008.
 *
 * v25.0 (2026-02-18, expires 2028-07-29). v26.0 shipped 2026-07-29 but is too
 * new to trust for a client touching ad spend. Review at the 12-month mark, or
 * sooner if a required field only exists in a later version.
 *
 * As of 2026-09-02 this pin serves two servers (marketing-performance and
 * page-engagement). Moving it is an ADR, not a local edit.
 */
export const GRAPH_API_VERSION = "v25.0"

export const GRAPH_API_HOST = "https://graph.facebook.com"

/**
 * Field selections ported from the Python reference. These encode which Graph
 * fields are actually useful per resource — the main thing worth carrying over
 * from that project.
 */
export const ACCOUNT_FIELDS = [
  "id",
  "name",
  "account_status",
  "currency",
  "timezone_name",
  "created_time",
  "amount_spent",
  "spend_cap",
  "business_id",
] as const

export const CAMPAIGN_FIELDS = [
  "id",
  "name",
  "status",
  "objective",
  "daily_budget",
  "lifetime_budget",
  "start_time",
  "stop_time",
  "created_time",
  "updated_time",
  "effective_status",
] as const

export const AD_SET_FIELDS = [
  "id",
  "name",
  "status",
  "campaign_id",
  "daily_budget",
  "lifetime_budget",
  "optimization_goal",
  "created_time",
  "updated_time",
] as const

export const AD_FIELDS = [
  "id",
  "name",
  "adset_id",
  "status",
  "created_time",
  "updated_time",
  "effective_status",
] as const

/**
 * Creative fields needed to resolve an ad to the Page post behind it.
 * `effective_object_story_id` resolves to the real post even when the creative
 * was defined inline; `object_story_id` is the explicit reference.
 */
export const AD_CREATIVE_FIELDS = ["id", "effective_object_story_id", "object_story_id"] as const

export const INSIGHT_METRICS = [
  "impressions",
  "clicks",
  "spend",
  "reach",
  "frequency",
  "actions",
  "conversions",
  "action_values",
  "ctr",
  "cpc",
  "cpp",
] as const

/** Default page size requested from Graph. */
export const DEFAULT_PAGE_SIZE = 100

/** Graph rejects larger page sizes on most edges. */
export const MAX_PAGE_SIZE = 500

/** Hard stop on pagination so a broken cursor cannot loop forever. */
export const DEFAULT_MAX_PAGES = 50

export const DEFAULT_TIMEOUT_MS = 30_000

/**
 * Graph error codes that indicate throttling. Callers should back off rather
 * than retry immediately.
 * @see https://developers.facebook.com/docs/graph-api/overview/rate-limiting
 */
export const THROTTLE_ERROR_CODES = new Set([4, 17, 32, 613])

/** Attempts for a retryable read, including the first. Writes never retry. */
export const DEFAULT_RETRY_ATTEMPTS = 3

/** First backoff delay; each subsequent attempt doubles it. */
export const DEFAULT_RETRY_BASE_MS = 500
